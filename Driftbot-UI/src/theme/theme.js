import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ff6600",
    },

    secondary: {
      main: "#f5f5f5",
    },

    background: {
      default: "#f8f8f8",
    },
  },
});

export default theme;